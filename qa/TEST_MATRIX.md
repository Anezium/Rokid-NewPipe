# Rokid-NewPipe QA Matrix

Generated by `$rokid-qa-generator`. Treat this as a starting point; tighten expectations after adding debug hooks.

## Modules

| Module | Role | Package/Namespace | Key Signals | Tests |
| --- | --- | --- | --- | ---: |
| `app` | glasses | `com.anezium.rokid.newpipe` | ar_ui, accessibility, input, player, search, wifi | 18 |

## Suggested Build Commands

- `.\gradlew.bat :app:testDebugUnitTest`
- `.\gradlew.bat :app:assembleDebug`

## Scenarios

| ID | Risk | Automation Layer | Why It Matters |
| --- | --- | --- | --- |
| `app-launch-smoke` | high | device smoke | Build, install, launch, and capture baseline state |
| `search-keyboard-swipe` | high | unit + device | Swipe left/right can reach Search and tap opens the Rokid keyboard |
| `offline-wifi-recovery` | high | device smoke | Offline empty state exposes a focusable Wi-Fi settings button |
| `player-rail-actions` | high | device smoke | Player controls appear only on demand and expose Play, Full, Quality, Speed, Subs, Close |
| `r08-accessibility-tree` | high | accessibility smoke | R08 Access Bridge sees usable clickable/focusable nodes with labels |

## Missing Hooks

- Add a debug-only DUMP_ROKID_STATE command with current focus id, focused text, keyboard state, player rail visibility, and selected player action.
- Add a tiny AccessibilityService test hook or uiautomator dump parser to assert every top-level command has text/contentDescription.
- Record real R08 key traces and feed them into `RokidKeyMapper` unit tests.
- Add a network-on fixture/device step for player quality/speed/subtitles, because offline glasses cannot load streams.

## Next Actions

1. Keep `scripts/rokid-regression.ps1` as the default local loop.
2. Convert each YAML scenario into a uiautomator assertion once the debug hooks exist.
3. Run with explicit `-GlassesSerial` when more than one ADB device is connected.
4. Re-run player scenarios after joining Wi-Fi; offline smoke can only prove the Wi-Fi recovery path.
